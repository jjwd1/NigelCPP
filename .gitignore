# Build output (but not checkpoints — those are training data)
out/*
!out/build/
out/build/*
!out/build/x64-Release/
out/build/x64-Release/*
!out/build/x64-Release/core/
out/build/x64-Release/core/*
!out/build/x64-Release/core/checkpoints/

# Visual Studio
.vs/
*.user

# LibTorch (too large, download separately)
GigaLearnCPP/libtorch/
libtorch_cpu/

# Bot DLL build output + runtime DLLs (from LibTorch, downloadable)
bot/out/
bot/*.dll
bot/*.exe

# Debug logs
bot_debug.log

# Plan files (local reference only)
plan_*.txt
